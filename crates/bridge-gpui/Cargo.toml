[package]
name = "notes-bridge-gpui"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lints]
workspace = true

# The shipped artifact is notes-gpui(.exe). The package name stays
# notes-bridge-gpui because it names the adapter, not the product.
[[bin]]
name = "notes-gpui"
path = "src/main.rs"

[dependencies]
notes-api = { workspace = true }
# Inherited, and it has to stay inherited: the workspace line turns off gpui's
# `windows-manifest` default feature, because that feature embeds a second
# RT_MANIFEST id=1 and rust-lld refuses an app manifest next to it (see build.rs).
# Repeating the version here would double-pin it; inheriting means that if the
# feature is ever re-enabled, this binary fails to LINK, loudly, and the reason is
# in the workspace comment next to the feature.
gpui = { workspace = true }
# Only to read the HWND out of the window gpui created: HasWindowHandle is
# implemented by gpui itself (src/window.rs), so this is the toolkit handle
# accessor, not a route around the port. No windows-rs type appears in this crate.
raw-window-handle = { workspace = true }
